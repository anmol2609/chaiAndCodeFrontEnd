import ReactDOM from "react-dom/client";
function App(){
    return (
        <div>hello baa hi so</div>
    )
}
const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(<App />);